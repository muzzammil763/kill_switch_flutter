name: kill_switch_example_mock
description: "Mock Firebase Example for kill_switch_flutter Package - No Firebase Setup Required"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: any 
  # Firebase dependencies removed for mock version
  # firebase_core: any
  
  # We still need the package for UI components (dialogs, etc.)
  kill_switch_flutter:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true